'use strict';

let chai = require('chai');
let expect = require('chai').expect;

describe('Payload', () => {
    it('returns a string', () => {
        var t = 'h1';
        
        expect(t).to.equal('h1');
        
    });
});